/**
 * ============================================
 *  PROJET JOUR 4 — Formation (squelette)
 * ============================================
 *
 * Implémente l'interface Affichable.
 *
 * Concepts utilisés :
 * -------------------
 * - Interface : implements Affichable (Jour 3)
 * - Encapsulation : private + getters/setters (Jour 2)
 * - toString() avec @Override (Jour 2)
 * - Validation avec throw (Jour 5)
 *
 * Attributs :
 * - code (String)
 * - nom (String)
 * - duree (int) — en heures
 */
public class Formation implements Affichable {

    // TODO : Déclarer les attributs (private)

    // TODO : Créer le constructeur
    //        Valider que duree > 0, sinon throw IllegalArgumentException

    // TODO : Implémenter afficherDetails() de Affichable
    //        Afficher : [FORMATION], code, nom, duree

    // TODO : Créer les getters (code, nom, duree)

    // TODO : Créer les setters avec validation
    //        setDuree(int duree) : si duree <= 0 → throw IllegalArgumentException

    // TODO : Redéfinir toString() avec @Override

}
